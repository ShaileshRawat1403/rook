# Providers

Rook can connect to model providers for local and remote AI workflows.

Provider setup is available through:

```bash
rook configure
```

or through the Rook terminal interface when no provider is configured.

Rook provider configuration is being migrated toward `ROOK_*` environment variables.
